[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -jar target/mrh-invoice-service-1.0.0.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
